name: Hello World Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: echo "👋 Hey StealthRagnar, you’re running your first GitHub Action!"
      
      - name: Show date
        run: date

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls -la
